# YAML for setting up a conda environment for
# the marx library
name: marx
channels:
  - conda-forge
  - defaults
dependencies:
  - beautifulsoup4
  - jupyter
  - matplotlib
  - pandas
  - python>=3.8.2
  - pyfiglet
  - pylint
  - requests
  - statsmodels
  - quantecon